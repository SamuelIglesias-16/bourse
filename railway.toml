[build]
builder = "NIXPACKS"
buildCommand = "pip install ."

[deploy]
startCommand = "uvicorn bourse.api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/watchlist"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
